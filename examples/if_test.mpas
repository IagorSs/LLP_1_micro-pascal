(* a program to check triangles' dimensions *)
program triangle;

const
  MSG = 'Enter the three sizes of a triangle: ';

var
  a, b, c;
  valid = 1;

begin
  if (a <= c) or (a + c <= b) or (b + c <= a) then
    begin
      writeln('is an invalid triangle');
      valid := 0
    end
end.
